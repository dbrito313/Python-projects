import random
import game_data


a_or_b = input("Who has more follower? Type 'A' or 'B': ").lower()



choices = print(f"Compare A:
score = 0


def scoring():
    global score
    if a_or_b == "a" and A > B:
        score += 1
        print(f"You're right! Current score : {score}")
